const initialState = {
    todos: [
        {
        todo: 'make bed',
        id: 1,
        completed: false}
    ],
};

export default function (state = initialState, action) {
    switch (action.type) {
    default:
    return state;
 
    }
}