const initialState = {
    todo: '',
    id: null,
    completed: false
};

export default function (state = initialState, action) {
    switch (action.type) {
       default:
       return state;
            
    }
}